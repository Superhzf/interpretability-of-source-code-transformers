###Details of models and tokenizers

model_type = roberta
model=roberta-base, microsoft/codebert-base, microsoft/graphcodebert-base, microsoft/unixcoder-base, huggingface/CodeBERTa-small-v1 all with corresponding tokenizers

model_type = bert
model= bert-base-uncased, CAUKiel/JavaBERT(bert-base-uncased tokenizer?)

model_type = gpt2
model= GPT2, 
